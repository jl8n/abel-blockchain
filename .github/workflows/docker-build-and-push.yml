name: Build and Push Docker Images

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push web image
        run: |
          docker build -t ghcr.io/${{ github.repository_owner }}/abeltracker-web:latest -f web/Dockerfile.web .
          docker push ghcr.io/${{ github.repository_owner }}/abeltracker-web:latest

      - name: Build and push server image
        run: |
          docker build -t ghcr.io/${{ github.repository_owner }}/abeltracker-server:latest -f server/Dockerfile.server .
          docker push ghcr.io/${{ github.repository_owner }}/abeltracker-server:latest

          